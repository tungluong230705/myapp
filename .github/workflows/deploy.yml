name: Deploy Docker App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy app
        run: |
          cd /home/lionelmessi/myapp
          git pull origin main
          docker compose down
          docker compose up -d --build